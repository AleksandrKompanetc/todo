import { Filter } from '../types'

interface FiltersProps {
  currentFilter: Filter
  onChange: (filter: Filter) => void
}

export default function Filters({ currentFilter, onChange }: FiltersProps) {

}